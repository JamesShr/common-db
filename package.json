{
  "name": "@jamesshr/common-db",
  "version": "1.0.0",
  "description": "common database table schema and column with typeorm",
  "main": "dist/index",
  "types": "dist/index",
  "scripts": {
    "test": "echo \"No test specified\"",
    "build": "rm dist && tsc",
    "prepublish": "npm run build"
  },
  "author": "JamesShr",
  "license": "MIT",
  "dependencies": {
    "typeorm": "^0.3.12"
  },
  "devDependencies": {
    "@types/node": "^18.14.0"
  }
}
